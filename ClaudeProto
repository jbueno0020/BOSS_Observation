<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BOSS Observation Tool</title>
    <link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;600;700&family=JetBrains+Mono:wght@500&display=swap" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/chartjs-plugin-datalabels@2"></script>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --bg-primary: #0f1419;
            --bg-secondary: #1a1f26;
            --bg-card: #232a33;
            --bg-hover: #2d3640;
            --text-primary: #f5f5f7;
            --text-secondary: #8b949e;
            --text-muted: #6e7681;
            --accent-blue: #4a90d9;
            --accent-green: #5cb85c;
            --accent-yellow: #d29922;
            --accent-orange: #db6d28;
            --accent-red: #f85149;
            --accent-purple: #a371f7;
            --border: #30363d;
            --shadow: rgba(0, 0, 0, 0.4);
        }

        body {
            font-family: 'DM Sans', -apple-system, BlinkMacSystemFont, sans-serif;
            background: var(--bg-primary);
            color: var(--text-primary);
            min-height: 100vh;
            line-height: 1.5;
        }

        .container {
            max-width: 1200px;
            margin: 0 auto;
            padding: 24px;
        }

        /* Header */
        .header {
            text-align: center;
            margin-bottom: 32px;
            padding-bottom: 24px;
            border-bottom: 1px solid var(--border);
        }

        .header h1 {
            font-size: 28px;
            font-weight: 700;
            margin-bottom: 8px;
            background: linear-gradient(135deg, var(--accent-blue), var(--accent-green));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
        }

        .header p {
            color: var(--text-secondary);
            font-size: 14px;
        }

        /* Setup Panel */
        .setup-panel {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
        }

        .setup-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
            gap: 20px;
            margin-bottom: 20px;
        }

        .form-group {
            display: flex;
            flex-direction: column;
            gap: 8px;
        }

        .form-group label {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .form-group input,
        .form-group select,
        .form-group textarea {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 12px 16px;
            color: var(--text-primary);
            font-size: 14px;
            font-family: inherit;
            transition: border-color 0.2s, box-shadow 0.2s;
        }

        .form-group input:focus,
        .form-group select:focus,
        .form-group textarea:focus {
            outline: none;
            border-color: var(--accent-blue);
            box-shadow: 0 0 0 3px rgba(74, 144, 217, 0.15);
        }

        .form-group textarea {
            resize: vertical;
            min-height: 100px;
        }

        /* Timer Display */
        .timer-section {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 32px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
            text-align: center;
        }

        .timer-display {
            font-family: 'JetBrains Mono', monospace;
            font-size: 72px;
            font-weight: 500;
            color: var(--text-primary);
            margin-bottom: 8px;
            letter-spacing: -2px;
        }

        .timer-display.recording {
            color: var(--accent-green);
            animation: pulse 1s ease-in-out infinite;
        }

        @keyframes pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .interval-indicator {
            font-size: 14px;
            color: var(--text-secondary);
            margin-bottom: 16px;
        }

        .interval-indicator span {
            color: var(--accent-blue);
            font-weight: 600;
        }

        .observation-counts {
            display: flex;
            justify-content: center;
            gap: 32px;
            margin-bottom: 16px;
            font-size: 13px;
        }

        .count-item {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .count-dot {
            width: 10px;
            height: 10px;
            border-radius: 50%;
        }

        .count-dot.target {
            background: var(--accent-blue);
        }

        .count-dot.peer {
            background: var(--accent-green);
        }

        .current-turn {
            display: inline-block;
            padding: 12px 28px;
            border-radius: 100px;
            font-size: 16px;
            font-weight: 600;
            margin-bottom: 24px;
            transition: all 0.3s ease;
        }

        .current-turn.target {
            background: rgba(74, 144, 217, 0.15);
            color: var(--accent-blue);
            border: 2px solid rgba(74, 144, 217, 0.4);
        }

        .current-turn.peer {
            background: rgba(92, 184, 92, 0.15);
            color: var(--accent-green);
            border: 2px solid rgba(92, 184, 92, 0.4);
        }

        .turn-pattern {
            font-size: 12px;
            color: var(--text-muted);
            margin-bottom: 20px;
        }

        .turn-pattern .active {
            color: var(--accent-blue);
            font-weight: 700;
        }

        .turn-pattern .active.peer {
            color: var(--accent-green);
        }

        .control-buttons {
            display: flex;
            gap: 12px;
            justify-content: center;
            flex-wrap: wrap;
        }

        .btn {
            padding: 14px 32px;
            border-radius: 10px;
            font-size: 14px;
            font-weight: 600;
            font-family: inherit;
            cursor: pointer;
            transition: all 0.2s ease;
            border: none;
            display: inline-flex;
            align-items: center;
            gap: 8px;
        }

        .btn-primary {
            background: var(--accent-blue);
            color: white;
        }

        .btn-primary:hover {
            background: #3d7fc4;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(74, 144, 217, 0.3);
        }

        .btn-danger {
            background: var(--accent-red);
            color: white;
        }

        .btn-danger:hover {
            background: #e5443d;
        }

        .btn-pause {
            background: var(--accent-yellow);
            color: #0f1419;
        }

        .btn-pause:hover {
            background: #c08a1e;
            transform: translateY(-1px);
            box-shadow: 0 4px 12px rgba(210, 153, 34, 0.3);
        }

        .btn-secondary {
            background: var(--bg-hover);
            color: var(--text-primary);
            border: 1px solid var(--border);
        }

        .btn-secondary:hover {
            background: #3d444d;
        }

        .btn:disabled {
            opacity: 0.5;
            cursor: not-allowed;
            transform: none !important;
        }

        /* Behavior Buttons */
        .behavior-section {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
        }

        .section-title {
            font-size: 12px;
            font-weight: 600;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 16px;
        }

        .behavior-categories {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 20px;
        }

        .category-group h4 {
            font-size: 11px;
            color: var(--text-muted);
            text-transform: uppercase;
            letter-spacing: 0.5px;
            margin-bottom: 12px;
            padding-bottom: 8px;
            border-bottom: 1px solid var(--border);
        }

        .category-group.on-task h4 {
            color: var(--accent-green);
            border-color: rgba(92, 184, 92, 0.3);
        }

        .category-group.off-task h4 {
            color: var(--accent-red);
            border-color: rgba(248, 81, 73, 0.3);
        }

        .behavior-grid {
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .behavior-btn {
            padding: 16px 20px;
            border-radius: 12px;
            font-size: 13px;
            font-weight: 600;
            font-family: inherit;
            cursor: pointer;
            transition: all 0.2s ease;
            border: 2px solid transparent;
            text-align: left;
            position: relative;
            overflow: hidden;
        }

        .behavior-btn:disabled {
            opacity: 0.4;
            cursor: not-allowed;
        }

        .behavior-btn .code {
            display: block;
            font-size: 18px;
            font-weight: 700;
            margin-bottom: 4px;
        }

        .behavior-btn .label {
            font-size: 11px;
            font-weight: 500;
            opacity: 0.8;
        }

        /* On-Task Behaviors */
        .btn-aet {
            background: rgba(92, 184, 92, 0.1);
            color: var(--accent-green);
            border-color: rgba(92, 184, 92, 0.3);
        }

        .btn-aet:hover:not(:disabled) {
            background: rgba(92, 184, 92, 0.2);
            border-color: var(--accent-green);
        }

        .btn-aet.selected {
            background: var(--accent-green);
            color: white;
        }

        .btn-pet {
            background: rgba(92, 184, 92, 0.1);
            color: #7dc97d;
            border-color: rgba(92, 184, 92, 0.2);
        }

        .btn-pet:hover:not(:disabled) {
            background: rgba(92, 184, 92, 0.2);
            border-color: #7dc97d;
        }

        .btn-pet.selected {
            background: #7dc97d;
            color: white;
        }

        /* Off-Task Behaviors */
        .btn-oft-m {
            background: rgba(248, 81, 73, 0.1);
            color: var(--accent-orange);
            border-color: rgba(248, 81, 73, 0.2);
        }

        .btn-oft-m:hover:not(:disabled) {
            background: rgba(248, 81, 73, 0.15);
            border-color: var(--accent-orange);
        }

        .btn-oft-m.selected {
            background: var(--accent-orange);
            color: white;
        }

        .btn-oft-v {
            background: rgba(248, 81, 73, 0.1);
            color: #e07070;
            border-color: rgba(248, 81, 73, 0.2);
        }

        .btn-oft-v:hover:not(:disabled) {
            background: rgba(248, 81, 73, 0.15);
            border-color: #e07070;
        }

        .btn-oft-v.selected {
            background: #e07070;
            color: white;
        }

        .btn-oft-p {
            background: rgba(248, 81, 73, 0.1);
            color: var(--accent-red);
            border-color: rgba(248, 81, 73, 0.3);
        }

        .btn-oft-p:hover:not(:disabled) {
            background: rgba(248, 81, 73, 0.2);
            border-color: var(--accent-red);
        }

        .btn-oft-p.selected {
            background: var(--accent-red);
            color: white;
        }

        /* Notes Section */
        .notes-section {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
        }

        /* Recording Section - Side by Side Layout */
        .recording-section {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
            transition: all 0.3s ease;
        }

        .recording-section.alert-active {
            border-color: var(--accent-yellow);
            box-shadow: 0 0 0 1px var(--accent-yellow);
        }

        .recording-section.alert-active.peer-alert {
            border-color: #66cdaa;
            box-shadow: 0 0 0 1px #66cdaa;
        }

        .recording-grid {
            display: grid;
            grid-template-columns: 1fr 1fr;
            gap: 24px;
        }

        .notes-panel {
            display: flex;
            flex-direction: column;
        }

        .notes-panel textarea {
            flex: 1;
            min-height: 280px;
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 8px;
            padding: 16px;
            color: var(--text-primary);
            font-size: 14px;
            font-family: inherit;
            resize: none;
            line-height: 1.6;
        }

        .notes-panel textarea:focus {
            outline: none;
            border-color: var(--accent-blue);
            box-shadow: 0 0 0 3px rgba(74, 144, 217, 0.15);
        }

        .notes-panel textarea::placeholder {
            color: var(--text-muted);
        }

        .behavior-panel {
            display: flex;
            flex-direction: column;
        }

        .behavior-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 12px;
        }

        .behavior-header .section-title {
            margin-bottom: 0;
        }

        /* Countdown Display */
        .countdown-display {
            display: flex;
            align-items: center;
            gap: 6px;
            background: var(--bg-secondary);
            padding: 8px 14px;
            border-radius: 8px;
            border: 1px solid var(--border);
        }

        .countdown-label {
            font-size: 11px;
            color: var(--text-muted);
        }

        .countdown-number {
            font-family: 'JetBrains Mono', monospace;
            font-size: 18px;
            font-weight: 600;
            color: var(--text-primary);
            min-width: 24px;
            text-align: center;
        }

        .countdown-display.warning {
            border-color: var(--accent-yellow);
            background: rgba(210, 153, 34, 0.1);
        }

        .countdown-display.warning .countdown-number {
            color: var(--accent-yellow);
        }

        .countdown-display.urgent {
            border-color: var(--accent-red);
            background: rgba(248, 81, 73, 0.1);
            animation: pulse-border 0.5s ease-in-out infinite;
        }

        .countdown-display.urgent .countdown-number {
            color: var(--accent-red);
        }

        @keyframes pulse-border {
            0%, 100% { border-color: var(--accent-red); }
            50% { border-color: var(--accent-orange); }
        }

        /* Record Alert Banner - Subtle with pulse */
        .record-alert {
            display: none;
            background: rgba(210, 153, 34, 0.15);
            border: 2px solid var(--accent-yellow);
            border-radius: 8px;
            padding: 10px 16px;
            margin-bottom: 12px;
            animation: subtle-pulse 1.5s ease-in-out infinite;
        }

        .record-alert.show {
            display: block;
        }

        @keyframes subtle-pulse {
            0%, 100% { opacity: 1; }
            50% { opacity: 0.7; }
        }

        .alert-content {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 10px;
            color: var(--accent-yellow);
            font-weight: 600;
        }

        .alert-icon {
            font-size: 16px;
        }

        .alert-text {
            font-size: 14px;
            letter-spacing: 0.5px;
        }

        .alert-subject {
            background: rgba(210, 153, 34, 0.2);
            padding: 3px 10px;
            border-radius: 100px;
            font-size: 12px;
        }

        .record-alert.peer-turn {
            background: rgba(102, 205, 170, 0.15);
            border-color: #66cdaa;
        }

        .record-alert.peer-turn .alert-content {
            color: #66cdaa;
        }

        .record-alert.peer-turn .alert-subject {
            background: rgba(102, 205, 170, 0.2);
        }

        /* Keyboard Hint */
        .keyboard-hint {
            margin-top: 16px;
            padding-top: 12px;
            border-top: 1px solid var(--border);
            font-size: 12px;
            color: var(--text-muted);
            text-align: center;
        }

        .keyboard-hint kbd {
            background: var(--bg-secondary);
            border: 1px solid var(--border);
            border-radius: 4px;
            padding: 2px 6px;
            font-family: 'JetBrains Mono', monospace;
            font-size: 11px;
            color: var(--text-primary);
        }

        /* Data Table */
        .data-section {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
        }

        .data-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 16px;
            flex-wrap: wrap;
            gap: 12px;
        }

        .data-stats {
            display: flex;
            gap: 24px;
            flex-wrap: wrap;
        }

        .stat-item {
            text-align: center;
        }

        .stat-value {
            font-size: 24px;
            font-weight: 700;
            color: var(--text-primary);
        }

        .stat-value.target {
            color: var(--accent-blue);
        }

        .stat-value.peer {
            color: var(--accent-green);
        }

        .stat-label {
            font-size: 11px;
            color: var(--text-secondary);
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .table-container {
            overflow-x: auto;
            border-radius: 8px;
            border: 1px solid var(--border);
            max-height: 300px;
            overflow-y: auto;
        }

        table {
            width: 100%;
            border-collapse: collapse;
            font-size: 13px;
        }

        th, td {
            padding: 12px 16px;
            text-align: left;
            border-bottom: 1px solid var(--border);
        }

        th {
            background: var(--bg-secondary);
            font-weight: 600;
            color: var(--text-secondary);
            font-size: 11px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            position: sticky;
            top: 0;
        }

        tr:last-child td {
            border-bottom: none;
        }

        tr:hover td {
            background: var(--bg-hover);
        }

        .badge {
            display: inline-block;
            padding: 4px 10px;
            border-radius: 100px;
            font-size: 11px;
            font-weight: 600;
        }

        .badge-target {
            background: rgba(74, 144, 217, 0.15);
            color: var(--accent-blue);
        }

        .badge-peer {
            background: rgba(92, 184, 92, 0.15);
            color: var(--accent-green);
        }

        .badge-on-task {
            background: rgba(92, 184, 92, 0.15);
            color: var(--accent-green);
        }

        .badge-off-task {
            background: rgba(248, 81, 73, 0.15);
            color: var(--accent-red);
        }

        /* Chart Section */
        .chart-section {
            background: var(--bg-card);
            border-radius: 16px;
            padding: 24px;
            margin-bottom: 24px;
            border: 1px solid var(--border);
        }

        .chart-wrapper {
            background: #ffffff;
            border-radius: 12px;
            padding: 20px;
            margin-bottom: 20px;
        }

        .chart-container {
            position: relative;
            height: 400px;
        }

        .chart-subtitle {
            text-align: center;
            font-size: 12px;
            color: #666;
            margin-top: 10px;
        }

        .chart-actions {
            display: flex;
            gap: 12px;
            justify-content: center;
            flex-wrap: wrap;
        }

        /* Toast Notification */
        .toast {
            position: fixed;
            bottom: 24px;
            right: 24px;
            background: var(--bg-card);
            border: 1px solid var(--border);
            border-radius: 12px;
            padding: 16px 24px;
            box-shadow: 0 8px 32px var(--shadow);
            transform: translateY(100px);
            opacity: 0;
            transition: all 0.3s ease;
            z-index: 1000;
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .toast.show {
            transform: translateY(0);
            opacity: 1;
        }

        .toast-success {
            border-left: 4px solid var(--accent-green);
        }

        .toast-icon {
            font-size: 20px;
        }

        /* Responsive */
        @media (max-width: 900px) {
            .recording-grid {
                grid-template-columns: 1fr;
            }

            .notes-panel textarea {
                min-height: 150px;
            }
        }

        @media (max-width: 768px) {
            .container {
                padding: 16px;
            }

            .timer-display {
                font-size: 48px;
            }

            .behavior-categories {
                grid-template-columns: 1fr;
            }

            .setup-grid {
                grid-template-columns: 1fr;
            }

            .observation-counts {
                flex-direction: column;
                gap: 8px;
            }

            .behavior-header {
                flex-direction: column;
                gap: 12px;
                align-items: flex-start;
            }
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 40px;
            color: var(--text-muted);
        }

        .empty-state-icon {
            font-size: 48px;
            margin-bottom: 16px;
            opacity: 0.5;
        }

        /* Interval Flash */
        .interval-flash {
            animation: flash 0.5s ease;
        }

        @keyframes flash {
            0%, 100% { background: var(--bg-card); }
            50% { background: rgba(74, 144, 217, 0.15); }
        }

        /* Print Styles */
        @media print {
            body {
                background: white;
                color: black;
            }
            
            .btn, .control-buttons, .behavior-section {
                display: none;
            }
            
            .chart-section, .data-section {
                break-inside: avoid;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <header class="header">
            <h1>📋 BOSS Observation Tool</h1>
            <p>Behavioral Observation of Students in Schools - Momentary Time Sampling</p>
        </header>

        <!-- Setup Panel -->
        <section class="setup-panel">
            <div class="setup-grid">
                <div class="form-group">
                    <label>Student Name</label>
                    <input type="text" id="studentName" placeholder="Enter student name">
                </div>
                <div class="form-group">
                    <label>Observer</label>
                    <input type="text" id="observerName" placeholder="Enter your name">
                </div>
                <div class="form-group">
                    <label>Date</label>
                    <input type="date" id="observationDate">
                </div>
                <div class="form-group">
                    <label>Duration (minutes)</label>
                    <select id="duration">
                        <option value="5">5 minutes</option>
                        <option value="10">10 minutes</option>
                        <option value="15" selected>15 minutes</option>
                        <option value="20">20 minutes</option>
                        <option value="30">30 minutes</option>
                    </select>
                </div>
            </div>
        </section>

        <!-- Timer Section -->
        <section class="timer-section" id="timerSection">
            <div class="timer-display" id="timerDisplay">00:00</div>
            <div class="interval-indicator">
                Interval: <span id="currentInterval">0</span> / <span id="totalIntervals">0</span>
            </div>
            <div class="observation-counts">
                <div class="count-item">
                    <span class="count-dot target"></span>
                    <span>Target Student: <strong id="targetCount">n=0</strong></span>
                </div>
                <div class="count-item">
                    <span class="count-dot peer"></span>
                    <span>Comparison Peer: <strong id="peerCount">n=0</strong></span>
                </div>
            </div>
            <div class="turn-pattern" id="turnPattern">
                Pattern: T T T T P • T T T T P • T T T T P ...
            </div>
            <div class="current-turn target" id="currentTurn">Waiting to Start</div>
            <div class="control-buttons">
                <button class="btn btn-primary" id="startBtn" onclick="startObservation()">
                    ▶ Start Observation
                </button>
                <button class="btn btn-pause" id="pauseBtn" onclick="pauseObservation()" disabled>
                    ⏸ Pause
                </button>
                <button class="btn btn-danger" id="stopBtn" onclick="stopObservation()" disabled>
                    ⏹ Stop
                </button>
                <button class="btn btn-secondary" onclick="resetObservation()">
                    ↻ Reset
                </button>
            </div>
        </section>

        <!-- Recording Section - Behaviors + Notes Side by Side -->
        <section class="recording-section" id="recordingSection">
            <div class="recording-grid">
                <!-- Notes on the left -->
                <div class="notes-panel">
                    <h3 class="section-title">Observation Notes</h3>
                    <textarea id="observationNotes" placeholder="Type observation notes here...&#10;&#10;• Classroom context&#10;• Activities occurring&#10;• Student behaviors&#10;• Interventions used&#10;&#10;(Notes auto-save as you type)"></textarea>
                </div>

                <!-- Behaviors on the right -->
                <div class="behavior-panel">
                    <div class="behavior-header">
                        <h3 class="section-title">Record Behavior</h3>
                        <div class="countdown-display" id="countdownDisplay">
                            <span class="countdown-label">Next interval in</span>
                            <span class="countdown-number" id="countdownNumber">15</span>
                            <span class="countdown-label">sec</span>
                        </div>
                    </div>
                    
                    <!-- Alert Banner -->
                    <div class="record-alert" id="recordAlert">
                        <div class="alert-content">
                            <span class="alert-icon">⚡</span>
                            <span class="alert-text">RECORD NOW!</span>
                            <span class="alert-subject" id="alertSubject">Target Student</span>
                        </div>
                    </div>

                    <div class="behavior-categories">
                        <div class="category-group on-task">
                            <h4>✓ On-Task</h4>
                            <div class="behavior-grid">
                                <button class="behavior-btn btn-aet" id="btn-AET" onclick="recordBehavior('AET')" disabled>
                                    <span class="code">1 AET</span>
                                    <span class="label">Active Engagement</span>
                                </button>
                                <button class="behavior-btn btn-pet" id="btn-PET" onclick="recordBehavior('PET')" disabled>
                                    <span class="code">2 PET</span>
                                    <span class="label">Passive Engagement</span>
                                </button>
                            </div>
                        </div>
                        <div class="category-group off-task">
                            <h4>✗ Off-Task</h4>
                            <div class="behavior-grid">
                                <button class="behavior-btn btn-oft-m" id="btn-OFT-M" onclick="recordBehavior('OFT-M')" disabled>
                                    <span class="code">3 OFT-M</span>
                                    <span class="label">Motor</span>
                                </button>
                                <button class="behavior-btn btn-oft-v" id="btn-OFT-V" onclick="recordBehavior('OFT-V')" disabled>
                                    <span class="code">4 OFT-V</span>
                                    <span class="label">Verbal</span>
                                </button>
                                <button class="behavior-btn btn-oft-p" id="btn-OFT-P" onclick="recordBehavior('OFT-P')" disabled>
                                    <span class="code">5 OFT-P</span>
                                    <span class="label">Passive</span>
                                </button>
                            </div>
                        </div>
                    </div>
                    <div class="keyboard-hint">
                        <span>⌨️ Keyboard: Press <kbd>1</kbd>-<kbd>5</kbd> to record (only when not typing in notes)</span>
                    </div>
                </div>
            </div>
        </section>

        <!-- Data Section -->
        <section class="data-section">
            <div class="data-header">
                <h3 class="section-title">Observation Data</h3>
                <div class="data-stats">
                    <div class="stat-item">
                        <div class="stat-value target" id="targetOnTask">0%</div>
                        <div class="stat-label">Target On-Task</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value peer" id="peerOnTask">0%</div>
                        <div class="stat-label">Peer On-Task</div>
                    </div>
                    <div class="stat-item">
                        <div class="stat-value" id="totalObservations">0</div>
                        <div class="stat-label">Total Intervals</div>
                    </div>
                </div>
            </div>
            <div class="table-container">
                <table>
                    <thead>
                        <tr>
                            <th>Interval</th>
                            <th>Time</th>
                            <th>Subject</th>
                            <th>Behavior</th>
                            <th>Status</th>
                        </tr>
                    </thead>
                    <tbody id="dataTableBody">
                        <tr>
                            <td colspan="5" class="empty-state">
                                <div class="empty-state-icon">📊</div>
                                <div>No data yet. Start an observation to begin recording.</div>
                            </td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </section>

        <!-- Chart Section -->
        <section class="chart-section">
            <h3 class="section-title">Visual Summary</h3>
            <div class="chart-wrapper">
                <div class="chart-container">
                    <canvas id="behaviorChart"></canvas>
                </div>
                <div class="chart-subtitle" id="chartSubtitle">
                    Target Student n=0 • Comparison Peer n=0 • interval=15s
                </div>
            </div>
            <div class="chart-actions">
                <button class="btn btn-secondary" onclick="copyChartToClipboard()">
                    📋 Copy Chart
                </button>
                <button class="btn btn-secondary" onclick="downloadChart()">
                    ⬇ Download Chart
                </button>
                <button class="btn btn-secondary" onclick="exportData()">
                    📄 Export CSV
                </button>
            </div>
        </section>
    </div>

    <!-- Toast Notification -->
    <div class="toast toast-success" id="toast">
        <span class="toast-icon">✓</span>
        <span id="toastMessage">Action completed!</span>
    </div>

    <script>
        // State
        let isRunning = false;
        let isPaused = false;
        let timer = null;
        let currentSeconds = 0;
        let currentInterval = 0;
        let totalIntervals = 0;
        let cyclePosition = 0; // 0-4: positions in the 5-interval cycle (4 target, 1 peer)
        let waitingForInput = false;
        let observations = [];
        let chart = null;
        let secondsUntilNextInterval = 15;
        let isNotesFieldFocused = false;

        // Initialize
        document.addEventListener('DOMContentLoaded', () => {
            document.getElementById('observationDate').valueAsDate = new Date();
            updateTotalIntervals();
            initChart();
            
            document.getElementById('duration').addEventListener('change', updateTotalIntervals);

            // Track if notes field is focused
            const notesField = document.getElementById('observationNotes');
            notesField.addEventListener('focus', () => {
                isNotesFieldFocused = true;
            });
            notesField.addEventListener('blur', () => {
                isNotesFieldFocused = false;
            });

            // Keyboard shortcuts (1-5 for behaviors)
            document.addEventListener('keydown', (e) => {
                // Don't trigger shortcuts if typing in notes or any input field
                if (isNotesFieldFocused || e.target.tagName === 'INPUT' || e.target.tagName === 'TEXTAREA') {
                    return;
                }

                // Only work when waiting for input
                if (!waitingForInput) return;

                const keyMap = {
                    '1': 'AET',
                    '2': 'PET',
                    '3': 'OFT-M',
                    '4': 'OFT-V',
                    '5': 'OFT-P'
                };

                if (keyMap[e.key]) {
                    e.preventDefault();
                    recordBehavior(keyMap[e.key]);
                }
            });
        });

        function updateTotalIntervals() {
            const duration = parseInt(document.getElementById('duration').value);
            // 15-second intervals = 4 intervals per minute
            totalIntervals = duration * 4;
            document.getElementById('totalIntervals').textContent = totalIntervals;
        }

        function isTargetTurn() {
            // Cycle: T T T T P (positions 0-3 are target, position 4 is peer)
            return cyclePosition < 4;
        }

        function initChart() {
            const ctx = document.getElementById('behaviorChart').getContext('2d');
            
            Chart.register(ChartDataLabels);
            
            chart = new Chart(ctx, {
                type: 'bar',
                data: {
                    labels: ['% AET', '% PET', '% TOTAL', '% OFT-M', '% OFT-V', '% OFT-P', '% TOTAL'],
                    datasets: [
                        {
                            label: 'Target Student',
                            data: [0, 0, 0, 0, 0, 0, 0],
                            backgroundColor: '#4a90d9',
                            borderColor: '#4a90d9',
                            borderWidth: 0,
                            borderRadius: 4,
                            barPercentage: 0.7,
                            categoryPercentage: 0.8
                        },
                        {
                            label: 'Comparison Peer',
                            data: [0, 0, 0, 0, 0, 0, 0],
                            backgroundColor: '#5cb85c',
                            borderColor: '#5cb85c',
                            borderWidth: 0,
                            borderRadius: 4,
                            barPercentage: 0.7,
                            categoryPercentage: 0.8
                        }
                    ]
                },
                options: {
                    responsive: true,
                    maintainAspectRatio: false,
                    plugins: {
                        title: {
                            display: true,
                            text: 'Behavioral Observation of Students in Schools',
                            color: '#333',
                            font: { 
                                family: 'DM Sans', 
                                size: 18,
                                weight: '600'
                            },
                            padding: { bottom: 20 }
                        },
                        legend: {
                            position: 'bottom',
                            labels: {
                                color: '#333',
                                font: { family: 'DM Sans', size: 12 },
                                padding: 20,
                                usePointStyle: true,
                                pointStyle: 'rect'
                            }
                        },
                        datalabels: {
                            anchor: 'end',
                            align: 'top',
                            color: '#333',
                            font: {
                                family: 'DM Sans',
                                size: 11,
                                weight: '600'
                            },
                            formatter: (value) => value > 0 ? value : ''
                        }
                    },
                    scales: {
                        x: {
                            grid: { display: false },
                            ticks: { 
                                color: '#333', 
                                font: { family: 'DM Sans', size: 11, weight: '500' }
                            }
                        },
                        y: {
                            beginAtZero: true,
                            max: 100,
                            title: {
                                display: true,
                                text: '% of Intervals Observed',
                                color: '#666',
                                font: { family: 'DM Sans', size: 12 }
                            },
                            grid: { color: 'rgba(0, 0, 0, 0.1)' },
                            ticks: { 
                                color: '#666', 
                                font: { family: 'DM Sans' },
                                stepSize: 10
                            }
                        }
                    }
                }
            });
        }

        function startObservation() {
            if (!document.getElementById('studentName').value) {
                showToast('Please enter a student name');
                return;
            }

            // If resuming from pause, don't reset values
            if (!isPaused) {
                currentSeconds = 0;
                currentInterval = 0;
                cyclePosition = 0;
            }

            isRunning = true;
            isPaused = false;

            document.getElementById('startBtn').disabled = true;
            document.getElementById('pauseBtn').disabled = false;
            document.getElementById('stopBtn').disabled = false;
            document.getElementById('timerDisplay').classList.add('recording');
            document.getElementById('pauseBtn').innerHTML = '⏸ Pause';

            updateTurnDisplay();
            enableBehaviorButtons(true);

            // Start the timer
            timer = setInterval(() => {
                if (!waitingForInput) {
                    currentSeconds++;
                    updateTimerDisplay();
                    updateCountdown();

                    // Every 15 seconds, prompt for observation
                    if (currentSeconds % 15 === 0 && currentSeconds > 0) {
                        promptForObservation();
                    }
                }
            }, 1000);
        }

        function updateCountdown() {
            secondsUntilNextInterval = 15 - (currentSeconds % 15);
            if (secondsUntilNextInterval === 15 && currentSeconds > 0) {
                secondsUntilNextInterval = 0;
            }

            const countdownEl = document.getElementById('countdownNumber');
            const countdownContainer = document.getElementById('countdownDisplay');
            
            countdownEl.textContent = secondsUntilNextInterval;

            // Update countdown styling based on time remaining
            countdownContainer.classList.remove('warning', 'urgent');
            if (secondsUntilNextInterval <= 3 && secondsUntilNextInterval > 0) {
                countdownContainer.classList.add('urgent');
            } else if (secondsUntilNextInterval <= 5) {
                countdownContainer.classList.add('warning');
            }
        }

        function promptForObservation() {
            currentInterval++;
            document.getElementById('currentInterval').textContent = currentInterval;

            // Play sound
            playIntervalSound();

            // Flash the section
            document.getElementById('timerSection').classList.add('interval-flash');
            setTimeout(() => {
                document.getElementById('timerSection').classList.remove('interval-flash');
            }, 500);

            // Show the alert banner
            const alertEl = document.getElementById('recordAlert');
            const alertSubject = document.getElementById('alertSubject');
            const recordingSection = document.getElementById('recordingSection');
            
            alertEl.classList.add('show');
            recordingSection.classList.add('alert-active');
            
            // Style based on target vs peer
            if (isTargetTurn()) {
                alertEl.classList.remove('peer-turn');
                recordingSection.classList.remove('peer-alert');
                alertSubject.textContent = '👤 Target Student';
            } else {
                alertEl.classList.add('peer-turn');
                recordingSection.classList.add('peer-alert');
                alertSubject.textContent = '👥 Comparison Peer';
            }

            waitingForInput = true;
            highlightBehaviorButtons(true);

            // Check if observation complete
            if (currentInterval >= totalIntervals) {
                setTimeout(() => {
                    stopObservation();
                    showToast('Observation complete!');
                }, 100);
            }
        }

        function playIntervalSound() {
            const audioContext = new (window.AudioContext || window.webkitAudioContext)();
            const oscillator = audioContext.createOscillator();
            const gainNode = audioContext.createGain();

            oscillator.connect(gainNode);
            gainNode.connect(audioContext.destination);

            oscillator.frequency.value = 800;
            oscillator.type = 'sine';
            
            gainNode.gain.setValueAtTime(0.3, audioContext.currentTime);
            gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.3);

            oscillator.start(audioContext.currentTime);
            oscillator.stop(audioContext.currentTime + 0.3);
        }

        function recordBehavior(behavior) {
            if (!waitingForInput) return;

            const observation = {
                interval: currentInterval,
                time: formatTime(currentSeconds),
                subject: isTargetTurn() ? 'Target' : 'Peer',
                behavior: behavior,
                isOnTask: behavior === 'AET' || behavior === 'PET'
            };

            observations.push(observation);
            updateDataTable();
            updateStats();
            updateChart();
            updateObservationCounts();

            // Hide the alert banner
            document.getElementById('recordAlert').classList.remove('show', 'peer-turn');
            document.getElementById('recordingSection').classList.remove('alert-active', 'peer-alert');

            // Remove highlight from buttons
            highlightBehaviorButtons(false);
            clearButtonSelections();

            // Highlight selected button briefly
            const btn = document.getElementById(`btn-${behavior}`);
            btn.classList.add('selected');
            setTimeout(() => btn.classList.remove('selected'), 300);

            // Move to next position in cycle
            cyclePosition = (cyclePosition + 1) % 5;
            updateTurnDisplay();

            // Reset countdown display
            document.getElementById('countdownNumber').textContent = '15';
            document.getElementById('countdownDisplay').classList.remove('warning', 'urgent');

            waitingForInput = false;
        }

        function updateObservationCounts() {
            const targetObs = observations.filter(o => o.subject === 'Target').length;
            const peerObs = observations.filter(o => o.subject === 'Peer').length;
            
            document.getElementById('targetCount').textContent = `n=${targetObs}`;
            document.getElementById('peerCount').textContent = `n=${peerObs}`;
            document.getElementById('chartSubtitle').textContent = 
                `Target Student n=${targetObs} • Comparison Peer n=${peerObs} • interval=15s`;
        }

        function pauseObservation() {
            if (isPaused) {
                // Resume
                startObservation();
            } else {
                // Pause
                isPaused = true;
                isRunning = false;
                clearInterval(timer);
                
                document.getElementById('startBtn').disabled = false;
                document.getElementById('pauseBtn').innerHTML = '▶ Resume';
                document.getElementById('timerDisplay').classList.remove('recording');
                
                enableBehaviorButtons(false);
                highlightBehaviorButtons(false);
                
                showToast('Observation paused');
            }
        }

        function stopObservation() {
            isRunning = false;
            isPaused = false;
            clearInterval(timer);
            
            document.getElementById('startBtn').disabled = false;
            document.getElementById('pauseBtn').disabled = true;
            document.getElementById('stopBtn').disabled = true;
            document.getElementById('timerDisplay').classList.remove('recording');
            document.getElementById('pauseBtn').innerHTML = '⏸ Pause';
            document.getElementById('recordAlert').classList.remove('show', 'peer-turn');
            document.getElementById('recordingSection').classList.remove('alert-active', 'peer-alert');
            
            enableBehaviorButtons(false);
            highlightBehaviorButtons(false);
        }

        function resetObservation() {
            stopObservation();
            currentSeconds = 0;
            currentInterval = 0;
            cyclePosition = 0;
            observations = [];
            waitingForInput = false;
            isPaused = false;
            secondsUntilNextInterval = 15;

            document.getElementById('timerDisplay').textContent = '00:00';
            document.getElementById('currentInterval').textContent = '0';
            document.getElementById('currentTurn').textContent = 'Waiting to Start';
            document.getElementById('currentTurn').className = 'current-turn target';
            document.getElementById('targetCount').textContent = 'n=0';
            document.getElementById('peerCount').textContent = 'n=0';
            document.getElementById('chartSubtitle').textContent = 
                'Target Student n=0 • Comparison Peer n=0 • interval=15s';
            document.getElementById('pauseBtn').innerHTML = '⏸ Pause';
            document.getElementById('countdownNumber').textContent = '15';
            document.getElementById('countdownDisplay').classList.remove('warning', 'urgent');
            document.getElementById('recordAlert').classList.remove('show', 'peer-turn');
            document.getElementById('recordingSection').classList.remove('alert-active', 'peer-alert');

            updateDataTable();
            updateStats();
            resetChart();
        }

        function updateTimerDisplay() {
            const minutes = Math.floor(currentSeconds / 60);
            const seconds = currentSeconds % 60;
            document.getElementById('timerDisplay').textContent = 
                `${minutes.toString().padStart(2, '0')}:${seconds.toString().padStart(2, '0')}`;
        }

        function formatTime(totalSeconds) {
            const minutes = Math.floor(totalSeconds / 60);
            const seconds = totalSeconds % 60;
            return `${minutes}:${seconds.toString().padStart(2, '0')}`;
        }

        function updateTurnDisplay() {
            const turnEl = document.getElementById('currentTurn');
            const patternEl = document.getElementById('turnPattern');
            
            if (isTargetTurn()) {
                turnEl.textContent = '👤 Observe TARGET Student';
                turnEl.className = 'current-turn target';
            } else {
                turnEl.textContent = '👥 Observe PEER';
                turnEl.className = 'current-turn peer';
            }

            // Update pattern display
            const positions = ['T', 'T', 'T', 'T', 'P'];
            const patternHTML = positions.map((p, i) => {
                const isActive = i === cyclePosition;
                const isPeer = p === 'P';
                if (isActive) {
                    return `<span class="active ${isPeer ? 'peer' : ''}">${p}</span>`;
                }
                return p;
            }).join(' ');
            
            patternEl.innerHTML = `Pattern: ${patternHTML} • ${patternHTML} • ${patternHTML} ...`;
        }

        function enableBehaviorButtons(enabled) {
            document.querySelectorAll('.behavior-btn').forEach(btn => {
                btn.disabled = !enabled;
            });
        }

        function highlightBehaviorButtons(highlight) {
            document.querySelectorAll('.behavior-btn').forEach(btn => {
                if (highlight) {
                    btn.style.animation = 'pulse 0.5s ease-in-out infinite';
                } else {
                    btn.style.animation = 'none';
                }
            });
        }

        function clearButtonSelections() {
            document.querySelectorAll('.behavior-btn').forEach(btn => {
                btn.classList.remove('selected');
            });
        }

        function updateDataTable() {
            const tbody = document.getElementById('dataTableBody');
            
            if (observations.length === 0) {
                tbody.innerHTML = `
                    <tr>
                        <td colspan="5" class="empty-state">
                            <div class="empty-state-icon">📊</div>
                            <div>No data yet. Start an observation to begin recording.</div>
                        </td>
                    </tr>
                `;
                return;
            }

            tbody.innerHTML = observations.map(obs => `
                <tr>
                    <td>${obs.interval}</td>
                    <td>${obs.time}</td>
                    <td><span class="badge badge-${obs.subject.toLowerCase()}">${obs.subject}</span></td>
                    <td><strong>${obs.behavior}</strong></td>
                    <td><span class="badge badge-${obs.isOnTask ? 'on-task' : 'off-task'}">${obs.isOnTask ? 'On-Task' : 'Off-Task'}</span></td>
                </tr>
            `).reverse().join('');
        }

        function updateStats() {
            const targetObs = observations.filter(o => o.subject === 'Target');
            const peerObs = observations.filter(o => o.subject === 'Peer');

            const targetOnTaskCount = targetObs.filter(o => o.isOnTask).length;
            const peerOnTaskCount = peerObs.filter(o => o.isOnTask).length;

            const targetPercent = targetObs.length > 0 
                ? Math.round((targetOnTaskCount / targetObs.length) * 100) 
                : 0;
            const peerPercent = peerObs.length > 0 
                ? Math.round((peerOnTaskCount / peerObs.length) * 100) 
                : 0;

            document.getElementById('targetOnTask').textContent = `${targetPercent}%`;
            document.getElementById('peerOnTask').textContent = `${peerPercent}%`;
            document.getElementById('totalObservations').textContent = observations.length;
        }

        function updateChart() {
            const targetObs = observations.filter(o => o.subject === 'Target');
            const peerObs = observations.filter(o => o.subject === 'Peer');

            // Calculate percentages for each behavior
            const calcPercent = (obs, behaviors) => {
                if (obs.length === 0) return 0;
                const count = obs.filter(o => behaviors.includes(o.behavior)).length;
                return Math.round((count / obs.length) * 100);
            };

            const targetData = [
                calcPercent(targetObs, ['AET']),           // % AET
                calcPercent(targetObs, ['PET']),           // % PET
                calcPercent(targetObs, ['AET', 'PET']),    // % TOTAL On-Task
                calcPercent(targetObs, ['OFT-M']),         // % OFT-M
                calcPercent(targetObs, ['OFT-V']),         // % OFT-V
                calcPercent(targetObs, ['OFT-P']),         // % OFT-P
                calcPercent(targetObs, ['OFT-M', 'OFT-V', 'OFT-P'])  // % TOTAL Off-Task
            ];

            const peerData = [
                calcPercent(peerObs, ['AET']),
                calcPercent(peerObs, ['PET']),
                calcPercent(peerObs, ['AET', 'PET']),
                calcPercent(peerObs, ['OFT-M']),
                calcPercent(peerObs, ['OFT-V']),
                calcPercent(peerObs, ['OFT-P']),
                calcPercent(peerObs, ['OFT-M', 'OFT-V', 'OFT-P'])
            ];

            chart.data.datasets[0].data = targetData;
            chart.data.datasets[1].data = peerData;
            chart.update();
        }

        function resetChart() {
            chart.data.datasets[0].data = [0, 0, 0, 0, 0, 0, 0];
            chart.data.datasets[1].data = [0, 0, 0, 0, 0, 0, 0];
            chart.update();
        }

        function copyChartToClipboard() {
            const canvas = document.getElementById('behaviorChart');
            canvas.toBlob(blob => {
                const item = new ClipboardItem({ 'image/png': blob });
                navigator.clipboard.write([item]).then(() => {
                    showToast('Chart copied to clipboard!');
                }).catch(err => {
                    showToast('Failed to copy chart');
                    console.error(err);
                });
            });
        }

        function downloadChart() {
            const canvas = document.getElementById('behaviorChart');
            const link = document.createElement('a');
            const studentName = document.getElementById('studentName').value || 'observation';
            const date = document.getElementById('observationDate').value;
            link.download = `BOSS_${studentName}_${date}.png`;
            link.href = canvas.toDataURL('image/png');
            link.click();
            showToast('Chart downloaded!');
        }

        function exportData() {
            if (observations.length === 0) {
                showToast('No data to export');
                return;
            }

            const studentName = document.getElementById('studentName').value;
            const observerName = document.getElementById('observerName').value;
            const date = document.getElementById('observationDate').value;
            const notes = document.getElementById('observationNotes').value;

            const targetObs = observations.filter(o => o.subject === 'Target');
            const peerObs = observations.filter(o => o.subject === 'Peer');

            const calcPercent = (obs, behaviors) => {
                if (obs.length === 0) return 0;
                const count = obs.filter(o => behaviors.includes(o.behavior)).length;
                return Math.round((count / obs.length) * 100);
            };

            let csv = 'BOSS Observation Data\n';
            csv += `Student,${studentName}\n`;
            csv += `Observer,${observerName}\n`;
            csv += `Date,${date}\n`;
            csv += `Target Observations,${targetObs.length}\n`;
            csv += `Peer Observations,${peerObs.length}\n\n`;
            
            csv += 'Raw Data\n';
            csv += 'Interval,Time,Subject,Behavior,On-Task\n';
            
            observations.forEach(obs => {
                csv += `${obs.interval},${obs.time},${obs.subject},${obs.behavior},${obs.isOnTask ? 'Yes' : 'No'}\n`;
            });

            // Add summary percentages
            csv += `\nSummary Percentages\n`;
            csv += `Behavior,Target %,Peer %\n`;
            csv += `AET,${calcPercent(targetObs, ['AET'])},${calcPercent(peerObs, ['AET'])}\n`;
            csv += `PET,${calcPercent(targetObs, ['PET'])},${calcPercent(peerObs, ['PET'])}\n`;
            csv += `Total On-Task,${calcPercent(targetObs, ['AET', 'PET'])},${calcPercent(peerObs, ['AET', 'PET'])}\n`;
            csv += `OFT-M,${calcPercent(targetObs, ['OFT-M'])},${calcPercent(peerObs, ['OFT-M'])}\n`;
            csv += `OFT-V,${calcPercent(targetObs, ['OFT-V'])},${calcPercent(peerObs, ['OFT-V'])}\n`;
            csv += `OFT-P,${calcPercent(targetObs, ['OFT-P'])},${calcPercent(peerObs, ['OFT-P'])}\n`;
            csv += `Total Off-Task,${calcPercent(targetObs, ['OFT-M', 'OFT-V', 'OFT-P'])},${calcPercent(peerObs, ['OFT-M', 'OFT-V', 'OFT-P'])}\n`;

            // Add notes
            if (notes) {
                csv += `\nObservation Notes\n`;
                csv += `"${notes.replace(/"/g, '""')}"\n`;
            }

            const blob = new Blob([csv], { type: 'text/csv' });
            const link = document.createElement('a');
            link.download = `BOSS_${studentName}_${date}.csv`;
            link.href = URL.createObjectURL(blob);
            link.click();
            showToast('Data exported to CSV!');
        }

        function showToast(message) {
            const toast = document.getElementById('toast');
            document.getElementById('toastMessage').textContent = message;
            toast.classList.add('show');
            setTimeout(() => toast.classList.remove('show'), 3000);
        }
    </script>
</body>
</html>
